# ---- PyTorch (install CUDA version matching your cluster) ----
# The cluster setup script handles this via: pip install torch --index-url ...
# If installing manually, see: https://pytorch.org/get-started/locally/
torch>=2.2.0

# ---- HuggingFace stack ----
transformers>=4.40.0
accelerate>=0.28.0
bitsandbytes>=0.43.0          # For 4-bit / 8-bit quantisation on large models
sentence-transformers>=2.6.0   # Embedding model for semantic metrics
huggingface-hub>=0.22.0

# ---- Scientific computing ----
numpy>=1.26.0
scipy>=1.12.0
scikit-learn>=1.4.0
pandas>=2.2.0

# ---- NLP metrics ----
nltk>=3.8.0
rouge-score>=0.1.2

# ---- Visualization ----
matplotlib>=3.8.0
seaborn>=0.13.0

# ---- Utilities ----
tqdm>=4.66.0
python-dotenv>=1.0.0
